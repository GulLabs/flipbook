---
"@gullabs/flipbook-core": patch
"@gullabs/react-flipbook": patch
---

Portrait back-curl, opaque folds, typed React binding, SSR-safe imports. Packages already versioned 3.0.0; this changeset is a patch so `changeset version` does not jump to 4.0.0.
